/*global $ */
/*jshint unused:false */
var app = window.app = window.app || {};
window.ENTER_KEY = 13;

$(function () {
	'use strict';

	// kick things off by creating the `App`
	new app.AppView();
});
