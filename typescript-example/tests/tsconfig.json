{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        "baseUrl": "..",

        // Ensure the 'intern' global is deifned
        "types": [ "intern" ]
    },
    "include": [
        "./**/*.ts"
    ]
}
