jquery-example
=============
## Setup

1. Install node modules and intern

    ```
    npm install
    ```
    
2. Setup `tunnelOptions` and `capabilities`.

## Running tests

* **Run tests in parallel**

    ```
    ./parallel.sh
    ```
